[railway]
  builder = "nixpacks"
  buildCommand = "npm ci --omit=dev && cd client && npm ci --omit=dev && npm run build"
  startCommand = "npm run start"
  nullables = ["DATABASE_URL"]

[nodejs]
  version = "20"

[railway.variable]
  NODE_ENV = "production"

[railway]
  builder = "nixpacks"
  buildCommand = "npm ci --omit=dev && cd client && npm ci --omit=dev && npm run build"
  startCommand = "npm run start"

[nodejs]
  version = "20"

[railway.variable]
  NODE_ENV = "production"
  DATABASE_URL = "postgresql://postgres:Gmc190494mcv@db.cpdrclazmvboenhlsccf.supabase.co:5432/postgres"